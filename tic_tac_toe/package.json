{
  "name": "tic_tac_toe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/game.test.js",
    "test:e2e": "npx playwright test --config=tests/e2e/playwright.config.js",
    "test:e2e:install": "npx playwright install chromium",
    "serve": "npx -y http-server -p 8765 -s --cors ."
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
