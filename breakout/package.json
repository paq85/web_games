{
  "name": "breakout",
  "version": "1.0.0",
  "description": "Neon arcade Breakout/Arkanoid brick breaker game",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "serve": "npx http-server -p 3000 -c-1"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1",
    "http-server": "^14.1.1",
    "jsdom": "^29.1.1",
    "vitest": "^3.1.1"
  }
}
