{
  "name": "tetris",
  "version": "1.0.0",
  "private": true,
  "description": "Tetris-like block puzzle game with neon aesthetic",
  "scripts": {
    "serve": "npx http-server -p 8080 -c-1",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
