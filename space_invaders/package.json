{
  "name": "space-invaders",
  "version": "1.0.0",
  "private": true,
  "description": "Modern arcade Space Invaders game",
  "scripts": {
    "serve": "npx http-server -p 8080 -c-1",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
